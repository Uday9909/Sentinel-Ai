[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N"]

[tool.ruff.lint.isort]
known-first-party = ["processor"]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["test_*.py"]
